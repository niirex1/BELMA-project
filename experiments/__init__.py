"""BELMA experiment scripts.

Each script in this package reproduces a specific table or figure from the
manuscript and writes its results to a JSON file in `results/`.

Run them via `bash scripts/reproduce_paper_results.sh` for the full suite,
or invoke individually as e.g. `python -m experiments.rq1_detection_repair`.
"""
